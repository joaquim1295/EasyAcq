# Dyno discovery report

No capture executed yet in this environment.

Run:

```powershell
python scripts/dyno_discovery.py --port COM6 --seconds 3
```

Then replace this file with generated output and confirm:
- selected baud/parity/stopbits/bytesize
- line terminator and frame structure
- parser rule for numeric extraction and unit mapping
